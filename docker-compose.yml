version: '3.8'

services:
  aa-scraper:
    build: .
    volumes:
      - ./output:/output
    command: >
      --origin LAX
      --destination JFK
      --date 2025-12-15
      --passengers 1
      --class economy
